<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Directory Index | Detail</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <div class="max-w-2xl mx-auto p-6 bg-white rounded-lg shadow-lg">
    <h1 class="text-3xl font-bold text-center text-gray-800">
      Detail (<%= values.length %>)
    </h1>

    <button onclick="window.history.back()" class="bg-blue-500 text-white px-4 py-2 rounded mt-4 hover:bg-blue-600">Back</button>

    <% if (values.length > 0) { %>
      <ol class="mt-6 space-y-2 list-decimal list-inside">
        <% values.forEach(value => { %>
          <li class="text-left">
            <a href="<%= value %>" class="text-blue-500 hover:underline">
              <%= value %>
            </a>
          </li>
        <% } ) %>
      </ol>      
    <% } else { %>
      <p class="text-6xl font-bold mt-8">Oops!</p>
    <% } %>

    <% if (values.length > 0) { %>
      <button onclick="window.history.back()" class="bg-blue-500 text-white px-4 py-2 rounded mt-4 hover:bg-blue-600">Back</button>
    <% } %>

  </div>
</body>
</html>
