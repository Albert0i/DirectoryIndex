<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Directory Index | Directory</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
  <div class="max-w-2xl mx-auto p-6 bg-white rounded-lg shadow-lg">
    <h1 class="text-3xl font-bold text-center text-gray-800">
      Directory (<%= hashes.length %>)
    </h1>

    <button onclick="window.history.back()" class="bg-blue-500 text-white px-4 py-2 rounded mt-4 hover:bg-blue-600">Back</button>

    <table class="min-w-full bg-white mt-6"> 
      <thead> 
        <tr> 
          <th class="w-1/4 px-4 py-2 text-left bg-gray-100">Id</th>
          <th class="w-1/4 px-4 py-2 text-left bg-gray-100">Name</th> 
          <th class="w-1/4 px-4 py-2 text-left bg-gray-100">Model</th> 
          <th class="w-1/4 px-4 py-2 text-left bg-gray-100">Capacity</th> 
          <th class="w-1/4 px-4 py-2 text-left bg-gray-100">Description</th> 
        </tr> 
      </thead> 
      <tbody> 
        <% hashes.forEach(hash => { %> 
          <tr class="border-t"> 
            <td class="px-4 py-2"><%= hash.id %></td>
            <td class="px-4 py-2"><%= hash.name %></td> 
            <td class="px-4 py-2"><%= hash.model %></td> 
            <td class="px-4 py-2"><%= hash.capacity %></td> 
            <td class="px-4 py-2"><%= hash.description %></td> 
          </tr> 
          <% }) %> 
      </tbody> 
    </table>

    <% if (hashes.length > 0) { %>
      <button onclick="window.history.back()" class="bg-blue-500 text-white px-4 py-2 rounded mt-4 hover:bg-blue-600">Back</button>
    <% } %>

  </div>
</body>
</html>
